# Optional. Copy to .env for `docker compose`. All have safe defaults.
# Google PageSpeed Insights key — raises PSI rate limits (25k/day). Optional.
PAGESPEED_API_KEY=

# Attended Firstlook staging exception (disabled by default).
# Never enable this for public visitor-submitted URLs.
FIRSTLOOK_STAGING_MODE=false
